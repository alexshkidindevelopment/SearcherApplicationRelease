@model IEnumerable<SearcherApplication.Models.DataModels.SearchResult>

@{
    ViewBag.Title = "Search Results";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.SearchMessage</h2>

<div class="divTable">
    <div class="headRow">
        <div class="divCell">
            <strong>@Html.DisplayNameFor(model => model.Title)</strong>
        </div>
        <div class="divCell">
            <strong>@Html.DisplayNameFor(model => model.Link)</strong>
        </div>
    </div>
    @foreach (var item in Model)
    {
        <div class="divRow">
            <div class="divCell">
                @Html.DisplayFor(modelItem => item.Title)
            </div>
            <div class="divCell">
                <a href="@item.Link">@Html.DisplayFor(modelItem => item.Link)</a>
            </div>
        </div>
    }
</div>

